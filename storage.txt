            // return /^([a-f\d])([a-f\d])([a-f\d])$/gi.test('0033ff');


            // return hex
            //             .replace(/(^#?)([a-f\d])([a-f\d])([a-f\d])$/i,
            //             'PPPPPPP');
                        // (h, r, g, b) => r + r + g + g + b + b);
                        // .match(/.{2}/g);
                        // .map(x => parseInt(x, 16));